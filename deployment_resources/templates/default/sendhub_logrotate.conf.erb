
<% node[:sendhub][:apps].each_key do |app| -%>
/mnt/var/log/hosts/<%= app %>.log
<% end -%>
{
	rotate 4
	maxsize 10M
	daily
	missingok
	notifempty
	delaycompress
	compress
	postrotate
		reload rsyslog >/dev/null 2>&1 || true
	endscript
}
